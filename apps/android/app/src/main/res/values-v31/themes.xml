<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12 and later draw their own splash on top of the launch window, with
  the launcher icon centred on windowSplashScreenBackground. iOS shows the bare
  colour and hands straight over to the wordmark, so the icon is dropped here
  for the same effect: one blue field from tap to wordmark, with nothing
  flashing in between.
-->
<resources>
  <style name="Theme.Mercato" parent="@android:style/Theme.Material.NoActionBar">
    <item name="android:windowBackground">@color/launch_background</item>
    <item name="android:colorBackground">@color/launch_background</item>
    <item name="android:navigationBarColor">@color/launch_background</item>
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:windowSplashScreenBackground">@color/launch_background</item>
    <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
  </style>
</resources>
