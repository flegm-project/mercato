package com.mercato.app

/// Links that must match what is filed with the store.
object AppLinks {
    /// Play requires a reachable privacy policy for an app that shows ads and
    /// sells an in-app purchase. The same document is published for iOS; the
    /// page picks its own language.
    const val PRIVACY_POLICY = "https://flegm.github.io/mercato/privacy"
}
