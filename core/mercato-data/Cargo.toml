[package]
name = "mercato-data"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Loads the Mercato dataset (CSV source of truth) into mercato-core types."

[dependencies]
mercato-core = { path = "../mercato-core" }
csv = "1"
serde = { workspace = true }
